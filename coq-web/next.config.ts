import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false // disables dev tools logo on web page in development
};

export default nextConfig;
